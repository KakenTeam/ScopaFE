import { DateTimeViPipe } from './date-time-vi.pipe';

describe('DateTimeViPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeViPipe();
    expect(pipe).toBeTruthy();
  });
});
